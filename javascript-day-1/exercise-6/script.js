const car = {
    brand : "Maruti",
    model : "M1982",
    year : 1982,
    color : "red"
}
console.log(car);
console.log(car.brand);
console.log(car.model);
console.log(car.year);
console.log(car.color);
console.log(`${car.brand} ${car.model} is ${car.color} and released in ${car.year}`);